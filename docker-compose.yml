version: '3'
services:
    node:
        build: .
        volumes:
            - ./:/usr/src/app
    nginx:
        image: 'nginx:stable-alpine'
        ports:
            - 1995:80
        volumes:
            - ./public:/usr/share/nginx/html
        depends_on:
            - node
